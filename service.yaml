apiVersion: v1
kind: Service
metadata:
  name: ajay-callboy
  labels:
    env: gowtham
    owner: prod
spec:
  type: LoadBalancer
  selector:
    env: gowtham
    owner: prod
  ports:
    - port: 8080
      targetPort: 8080
